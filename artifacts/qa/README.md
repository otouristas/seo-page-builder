# Verification evidence

Screenshots cover desktop and mobile public and application-demo screens. Demo numbers are explicit fixtures, not customer performance.

`report-fixture.pdf` and its PNG verify actual server PDF export with Greek text and long URL wrapping. The PDF is labeled QA data.

Lighthouse JSON files are local production-build mobile lab tests. They are not field measurements or a guarantee for every deployed route/network. Runtime provider status is recorded separately in `../provider-validation.json`; local passing tests do not mark hosted integrations operational.
