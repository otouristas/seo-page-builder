# Rankframe

SEO lab: βάλε URL, δες on-page + σκηνή Google SERP, κινήσεις για να ανέβεις. Όχι εγγύηση θέσης.

Πλήρης περιγραφή: [BUILD.md](./BUILD.md)

```
npm install
npm run dev
```

- `/` landing
- `/app` lab
- `/login` Sign in with Google
